# Daybridge 근무시간 기본값 조정

- 날짜: 2026-08-25 KST
- 범위: 점심·퇴근 경계와 시간표 기본 마감 시각

## 요청

- 점심 경계는 `11:30`
- 퇴근 경계와 시간표 기본 마감은 `18:00`

## 수정

- 시간표 저장소의 기본 `dayEnd`를 `22:00`에서 `18:00`으로 변경했다.
- 스케줄러에 설정이 없을 때 사용하는 fallback도 `18:00`으로 맞췄다.
- 브라우저 smoke fixture의 기본 설정과 설정창 기대값을 `18:00`으로 갱신했다.
- 기존 로컬 `schedule-settings.json`에 남아 있던 이전 기본값 `22:00`을 `18:00`으로 갱신했다.
- 점심 `11:30`, 오후 시작 `13:00`, 퇴근 `18:00` 카운트다운 경계는 기존 `workday-clock`에서 이미 사용 중인 값을 유지했다.

## 검증

- 저장소 기본값 테스트와 스케줄러 테스트를 실행한다.
- 대시보드 설정창에서 마감 시간이 `18:00`으로 보이는지 smoke로 확인한다.
- 오버레이 카운트다운의 점심·퇴근 경계 테스트를 포함해 전체 Node·브라우저·빌드 검증을 수행한다.
